# MentorMatch Frontend Environment Variables
# Copy this file to .env.local and fill in the values.
#
# No NEXT_PUBLIC_API_URL needed! API calls use relative URLs.
# In dev: Next.js rewrites proxy /api/* to localhost:8080
# In prod: Nginx reverse proxy routes /api/* to the backend container

# --- CLOUDFLARE TURNSTILE ---
NEXT_PUBLIC_TURNSTILE_SITE_KEY=your-turnstile-site-key

# --- GOOGLE OAUTH ---
NEXT_PUBLIC_GOOGLE_CLIENT_ID=your-google-client-id.apps.googleusercontent.com
